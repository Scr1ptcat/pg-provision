# pg-provision full implementation phases 1-9 — LEDGER

| phase | deliverable                    | code_status | live_status                     | evidence                                                                                                                                                                                                                                                            | notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| ----- | ------------------------------ | ----------- | ------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| start | run initialization             | code-green  | pending                         | `git rev-parse HEAD` -> `2a3b46dc3a14e09a4aa32978615d94822f3bf917`; `sudo -n true` -> unavailable; `psql --version` -> `18.3`                                                                                                                                       | No prior `STATE.md`; started Phase 1.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| 1     | PG 18 readiness and version UX | code-green  | live-pass-with-optional-blocker | `PYTHONPATH=src pytest -q tests/test_config.py tests/test_cli.py` -> 15 passed; `pre-commit run --all-files` -> passed; `phase-1-dry-run.log`; `phase-1-help.log`                                                                                                   | `--pg-version 18 --dry-run` exits 0 and help lists `--pg-version`; optional privileged PGDG install smoke blocked because `sudo -n true` fails on this host.                                                                                                                                                                                                                                                                                                                                                                                  |
| 2     | pgvector + conditional SPL     | code-green  | blocked                         | `PYTHONPATH=src pytest -q` -> 57 passed; `pre-commit run --all-files` -> passed; `phase-2-code-gate.log`; `phase-2-pgvector-dry-run.log`                                                                                                                            | pgvector package hooks, extension SQL, backend contract, and conditional SPL are covered by unit gates. Live PGDG pgvector smoke blocked because `sudo -n true` fails on this host.                                                                                                                                                                                                                                                                                                                                                           |
| 3     | logical destroy                | code-green  | blocked                         | `uv run pytest -q` -> 78 passed; `uv run pre-commit run --all-files` -> passed; `docs/run/full-implementation-phases-1-9/logs/phase-3-implementation.log`                                                                                                           | Destroy flags, final-value confirmation, manifest-only dry-run, port/socket targeting, stdin SQL execution, protected target guards, and destroy-only dispatch are covered. Privileged live command `sudo -n pgprovision --destroy-db pgprov_destroy_smoke --confirm-destroy-db pgprov_destroy_smoke --destroy-only` blocked because `sudo -n true` exits 1 on this host.                                                                                                                                                                     |
| 4     | runtime abstraction            | code-green  | blocked                         | `uv run pytest -q` -> 89 passed; `uv run pre-commit run --all-files` -> passed; `uv run pgprovision --pg-version 18 --repo pgdg --listen-addresses localhost --dry-run` -> manifest only; `docs/run/full-implementation-phases-1-9/logs/phase-4-implementation.log` | `lib/runtime.sh`, `os_self_heal` backend contract, manifest-only dry-run, fatal missing profiles, and Ubuntu valid-PGDATA relocation refusal are covered by unit gates. Privileged double-run command `sudo -n pgprovision --pg-version 18 --repo pgdg --listen-addresses localhost` blocked because `sudo -n true` exits 1 on this host.                                                                                                                                                                                                     |
| 5     | user-mode MVP                  | code-green  | blocked                         | `uv run pytest -q` -> 117 passed; `uv run pre-commit run --all-files` -> passed; `docs/run/full-implementation-phases-1-9/logs/phase-5-implementation.log`                                                                                                          | User-mode CLI/shell no-sudo dispatch, `os/user.sh`, runtime user psql/file behavior, user-owned HBA/config/stamp paths, fail-fast MVP limits, and no-root user-mode guard are covered. Live command `BASE="$PWD/.copilot-live/phase5-user"; rm -rf "$BASE"; mkdir -p "$BASE"; uv run pgprovision --user-mode --user-base-dir "$BASE" --data-dir "$BASE/data" --port 55432 --listen-addresses localhost --create-db pgprov_user_smoke --create-user pgprov_user_smoke` blocked because `postgres`, `initdb`, and `pg_ctl` are missing on PATH. |
| 6     | user-mode parity               | code-green  | blocked                         | `uv run pytest -q` -> 138 passed; `uv run pre-commit run --all-files` -> passed; `docs/run/full-implementation-phases-1-9/logs/phase-6-implementation.log`                                                                                                          | User-mode TLS validation, pgss/pgvector control checks and extension SQL, logical destroy, socket-only/group/map behavior, admin DB-role operations, and PGDATA validation are covered. Live parity command on port `55433` blocked because `postgres`, `initdb`, and `pg_ctl` are missing on PATH; `psql` is `/usr/bin/psql`.                                                                                                                                                                                                                |
| 7     | tarball bootstrap              | code-green  | blocked                         | `uv run pytest -q` -> 146 passed; `uv run pre-commit run --all-files` -> passed; `docs/run/full-implementation-phases-1-9/logs/phase-7-implementation.log`                                                                                                          | Bootstrap tarball flags, SHA256-required validation for local/URL sources, safe extraction, binary discovery, `--bootstrap-only`, and user backend handoff are covered. Live bootstrap command on port `55434` blocked because `PGPROVISION_TEST_TARBALL` and `PGPROVISION_TEST_TARBALL_SHA256` are unset; operator confirmed it will remain blocked.                                                                                                                                                                                         |
